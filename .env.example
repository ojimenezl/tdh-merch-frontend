# Configuración del frontend TDH merch
# Copia este archivo como .env y ajusta los valores.

# Usar datos mock (true) o el backend real (false).
VITE_USAR_MOCK=true

# URL base del backend Nest (se usa cuando VITE_USAR_MOCK=false).
VITE_API_URL=http://localhost:3000/api
